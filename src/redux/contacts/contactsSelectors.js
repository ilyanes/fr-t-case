export const getContacts = (state) => state.contacts.contacts;
export const isLoading = (state) => state.contacts.loading;
export const numberPage = (state) => state.contacts.page;
export const isMore = (state) => state.contacts.hasMore;
export const numberLimit = (state) => state.contacts.limit;
